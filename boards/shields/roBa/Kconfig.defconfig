if SHIELD_ROBA_R

config ZMK_KEYBOARD_NAME
    default "roBa_R"

config ZMK_SPLIT
    default y

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_ROBA_L

config ZMK_KEYBOARD_NAME
    default "roBa_L"

config ZMK_SPLIT
    default y

endif

if SHIELD_ROBA_DONGLE

config ZMK_KEYBOARD_NAME
    default "roBa_dongle"

# キーマップを空にすることで回避
config ZMK_KEYMAP
    string
    default ""

endif
